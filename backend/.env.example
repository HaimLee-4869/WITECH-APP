DATABASE_URL=sqlite:///./signid.db
AI_DEVICE=cpu
LOG_LEVEL=INFO
CORS_ORIGINS=*

# false(운영 설정, 2026-09-16 팀 결정): 앱이 보낸 gestureId로 템플릿을 조회한다.
#   분류 모델은 호출하되 결과를 auth_logs.predicted_gesture_id에 기록만 하고 판정에 쓰지 않는다.
# true(명세 7장): classify_gesture() 결과로 템플릿을 조회한다.
USE_GESTURE_CLASSIFIER=false
