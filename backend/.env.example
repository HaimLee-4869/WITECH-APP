DATABASE_URL=sqlite:///./signid.db
AI_DEVICE=cpu
LOG_LEVEL=INFO
CORS_ORIGINS=*

# true(기본, 명세 7장): classify_gesture() 결과로 템플릿을 조회한다.
# false: 앱이 보낸 gestureId로 템플릿을 조회한다. 분류는 호출해 기록만 한다.
USE_GESTURE_CLASSIFIER=true
