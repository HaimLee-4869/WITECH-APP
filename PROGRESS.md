# Sign-ID 진행 현황

> 수어 제스처 기반 비접촉 인증 시스템. 이 문서는 저장소의 실제 코드·커밋·측정 기록을 근거로
> 2026-09-16 기준 상태를 정리한 것이다.
>
> 근거 문서: [`CHALLENGE_SPEC.md`](CHALLENGE_SPEC.md), [`BACKEND_SPEC.md`](BACKEND_SPEC.md),
> [`README.md`](README.md)(앱), [`backend/README.md`](backend/README.md),
> [`challenge_response/README.md`](challenge_response/README.md),
> [`backend/ai/AI_RELEASE_README.md`](backend/ai/AI_RELEASE_README.md)

---

## 1. 시스템 개요

카메라 앞에서 **수어 동작을 수행하면 본인인지 판정하는 비접촉 인증 시스템**이다. 지문이나 카드
대신 사용자가 미리 등록한 수어 제스처를 수행하고, 서버가 그 동작의 임베딩을 등록 템플릿과
비교해 출입을 승인한다. 동작 자체가 암호이면서 동시에 생체 특징이다. 녹화 영상을 재생하는
replay attack을 막기 위해, 인증 때마다 시스템이 무작위 동작을 요청하고 실제로 반응했는지
확인하는 Challenge–Response 모듈을 별도로 두었다.

### 전체 인증 흐름

```
① Challenge 제시      서버가 무작위 동작 3개를 요청 (손 모양 2 + 방향 이동 1)
        ↓
② 사용자 수행         앱이 카메라로 MediaPipe 랜드마크 21점 × 프레임 수집
        ↓
③ 안티스푸핑 판정     요청한 동작을 제한 시간 안에 했는지 규칙 기반 판정  ← challenge_response/
        ↓
④ 임베딩 비교         AI 인코더가 128차원 임베딩 생성, 등록 템플릿과 코사인 유사도  ← backend/ + ai/
        ↓
⑤ 승인/거부           서버 threshold와 비교해 판정하고 이력 기록
```

⚠️ **③과 ④는 아직 한 흐름으로 합쳐지지 않았다.** Challenge 모듈은 독립 프로토타입(파이썬 +
웹캠)이고, 앱–백엔드–AI 경로는 ④⑤만 연결돼 있다. 통합은 미착수다(7장).

### 구성 요소와 경계

| 구성 요소 | 위치 | 역할 | 하지 않는 것 |
|---|---|---|---|
| **Flutter 앱** | `lib/` | 카메라 촬영, MediaPipe 랜드마크 추출, **원본 좌표 그대로** 서버 전송, 결과 표시 | 정규화·리샘플링·특징 추출, 판정, threshold 보관 |
| **FastAPI 백엔드** | `backend/` | 사용자·등록·이력 관리, 코사인 유사도 계산, threshold 비교, 재색인 | 모델 추론(AI 모듈에 위임), 전처리 직접 구현 |
| **AI 모델** | `backend/ai/` (AI팀 전달본) | 랜드마크 → 128차원 임베딩, 제스처 분류 | 유사도 계산, 템플릿 생성, 판정 |
| **안티스푸핑** | `challenge_response/` | Challenge 생성과 규칙 기반 수행 판정 | 본인 확인(누구인지는 판정하지 않음) |

경계 원칙 두 가지가 전체를 지배한다.

- **전처리는 한 곳에서만 한다.** 앱은 MediaPipe 원본 좌표를 그대로 보내고, 정규화·리샘플링·
  feature 생성은 전부 AI 모듈(`ai/features.py`)이 한다. 앱이나 백엔드가 같은 계산을 다시 구현하면
  학습 때와 미세하게 달라져 정확도가 조용히 깎인다.
- **판정에 쓰는 값은 서버가 소유한다.** threshold, 등록 회차 수, 촬영 시간은 전부 DB와
  `GET /config`에서 온다. 앱에는 상수로 존재하지 않는다.

---

## 2. 안티스푸핑 (`challenge_response/`)

### 무엇이고 왜 필요한가

제스처 인증만 있으면 **사용자가 동작하는 모습을 찍은 영상을 카메라 앞에 재생하는 것만으로
뚫린다.** 동작은 지문과 달리 밖으로 드러나기 때문이다. 그래서 인증할 때마다 시스템이 무작위
동작 3개(손 모양 2개 + 방향 이동 1개)를 요청하고, 제한 시간 안에 수행했는지 판정한다. 미리
찍어둔 영상은 요청을 예측할 수 없다.

딥러닝을 쓰지 않는다. MediaPipe 랜드마크 + 기하 계산 + 상태 머신으로 구현했다.

| 항목 | 값 | 근거 |
|---|---|---|
| 단계 구성 | 손 모양 2 + 이동 1 (총 3단계) | `CHALLENGE_SPEC.md` |
| 손 모양 종류 | OPEN_PALM, FIST, INDEX, TWO_FINGERS | |
| 이동 방향 | 좌/우/상/하 | `direction_map`, 16개 영상 만장일치 |
| 단계 제한 시간 | 2000ms | 손 모양 확정 시간 + 획 소요시간 p95 = 1831ms (500ms 올림) |
| 전체 제한 시간 | 6000ms | 단계 시간 × 3 |
| 재시도 | 1회 | **측정값 아님.** 프로토콜 정책 |

(위 값은 `configs/challenge_config.json` 실측. README 3장 표는 2명 기준 옛 값이므로 config가 기준이다.)

### 데이터 수집 규모

| 항목 | 값 |
|---|---|
| 참가자 | 5명 (P01~P05) |
| 참가자당 영상 | 26개 (정상 동작 + 경계 반례 `NEG_*`) |
| 총 영상 | **130개** (`data/landmarks/*.npz` 실측) |
| 초기 파일럿 | 2명 × 26 = 52개 (2026-09-07) → 5명으로 확장(2026-09-16) |
| 웹캠 반례 녹화 | P01 15회 (흔들기·대각선·손 이탈 각 5회) |
| 실시간 세션 | P01 77회 (`data/sessions/results.csv`) |

### 임계값을 데이터에서 도출한 방식

이 모듈의 핵심 설계는 **"임계값을 상식으로 정하지 않는다"** 이다.

```
영상 → 랜드마크 추출(01) → 좌우 반전 확정(02) → 분포 측정(03)
     → 분포에서 임계값 도출(04) → 규칙 검증(05)
```

- 코드에 임계값 숫자 리터럴이 없다. 전부 `configs/challenge_config.json` 한 곳에 있고,
  각 값에 근거 분포가 `_source`로 적혀 있다.
- 도출 규칙 자체도 데이터가 아니라 정책이므로 `configs/derivation_policy.json`에 분리했다
  (정상 p5를 통과시키고 경계 반례 p95를 거르는 방식).
- **분포가 겹쳐 도출하지 못한 값은 임의로 채우지 않고 `null` + `_unresolved`로 남긴다.**
  실행 시 경고가 뜬다.
- 측정으로 정할 수 없는 값(재시도 횟수, 좌표계)은 `_source`에 "측정값 아님"을 명시한다.

주요 임계값 (현재 `configs/challenge_config.json`, 5명 130개 영상 기준):

| 항목 | 값 | 근거 |
|---|---|---|
| `finger_extended_angle.others` | 135.9° | 펴야 하는 손가락 p5=166.4° / 접어야 하는 손가락 p95=105.5° (틈 61.0°) |
| `finger_extended_angle.thumb` | 148.9° | OPEN_PALM 엄지 p5=154.5° / FIST 엄지 p95=143.4° (틈 11.1°) |
| `fist_max_tip_wrist_ratio` | 0.909 | FIST 정답 p95=0.835 / FIST로 오판정된 NEG_halffist p5=0.983 |
| `shape_hold_frames` | 7 | 반례 오검출 연속길이 p95=5.9 + 1 |
| `escape_frames` | 14 | 오검출 구간 **최대치** 13 + 1 (아래 설명) |
| `movement.window_ms` | 600 | 획 1회 소요시간 p5=608ms |
| `movement.min_displacement_ratio` | 0.226 | MOVE 영상별 p75 변위의 p5=0.366 / NEG_shake p95=0.086 |
| `movement.axis_dominance_ratio` | 3.634 | MOVE 획 주축비 p5=4.38 / NEG_diagonal p95=2.89 |
| `tracking.min_detection_score` | 0.938 | 정상 영상 검출 프레임 p5 (21,885프레임) |
| `tracking.max_lost_frames` | 38 | **도출 실패.** 정상 영상 중간 끊김이 1건뿐. 임시값(측정값 아님) |
| `shape_confidence_min` | **null** | **도출 실패.** 오검출 프레임 신뢰도 p95=0.918 ≥ 정답 p5=0.824 |

`escape_frames`만 p95가 아니라 **최대치**(관측 구간 28개 중 최대 13프레임)를 쓴다. 다른 임계값은 사용성 기준이라 드물게 어긋나도
사용자가 다시 시도하면 되지만, 이탈 관문은 잡음 구간 하나가 관문을 열면 그 시도의 방어가 통째로
무력해지기 때문이다.

### 05 검증 결과 — SPEC 4.11 목표 10개 중 **9개 달성**

| 항목 | 2명 기준 | 5명 기준 | 목표 | 판정 |
|---|---|---|---|---|
| OPEN_PALM 정확 판정 | 99.8% | 99.9% | ≥90% | 달성 |
| FIST 정확 판정 | 99.3% | 98.2% | ≥90% | 달성 |
| INDEX 정확 판정 | 99.8% | 99.3% | ≥90% | 달성 |
| TWO_FINGERS 정확 판정 | 98.1% | 98.2% | ≥90% | 달성 |
| NEG_halffist → OPEN_PALM/FIST | 91.2% | **20.6%** | <5% | **미달** |
| NEG_threefingers → TWO_FINGERS | 0.3% | 0.6% | <5% | 달성 |
| NEG_indexring → TWO_FINGERS | 1.9% | 0.0% | <5% | 달성 |
| MOVE_* 이동 검출 | 16/16 영상 | 편도 첫 검출 26/26회 | 전부 | 달성 |
| NEG_shake 이동 검출 | 0회 | 0회 | 0회 | 달성 |
| NEG_diagonal 요청 1회 통과율 | – | 15.0% (3/20쌍) | ≤25% | 달성 |

미달한 `NEG_halffist`(반쯤 쥔 손)는 **사람마다 반쯤 쥐는 방식이 두 가지**여서 생긴 문제다.
손가락 중간 마디를 굽히는 사람(P01, P05)과 손바닥 쪽 관절만 굽히는 사람(P02~P04)이 있는데,
각도 규칙은 후자를 편 손으로 읽는다. FIST 쪽은 손끝–손목 거리 게이트로 86.5% → 0.6%까지 막았고
(단, 같은 영상으로 임계값을 정하고 평가한 낙관적 수치), **OPEN_PALM 쪽 20.0%는 수용했다.**
막으려고 하한을 걸면 정상 OPEN_PALM 정답률이 99.9% → 89.4%로 떨어져 얻는 것보다 잃는 것이 크다.

### 실시간 테스트 결과 (P01 웹캠 77회, `results.csv` 실측)

| 규칙 버전 | 시도 | 통과 | 비고 |
|---|---|---|---|
| `2026-09-16.fist-tip-gate` | 43회 | 29회 (67%) | 크고 빠른 동작 |
| `2026-09-16.time-based-frames` | 34회 | 33회 (**97%**) | 프레임 수 대신 관측 시각으로 판정하도록 수정한 뒤 |
| **합계** | **77회** | **62회 (81%)** | 통과 소요 중앙값 4,260ms |

실패 사유: `ACTION_TIMEOUT` 7, `TOTAL_TIMEOUT` 5, `HAND_NOT_FOUND` 2, `TRACKING_UNSTABLE` 1.

개선 과정에서 드러난 문제 두 가지가 컸다.

1. **초기 20회 중 2회만 통과.** 실패 11건 중 7건이 "손을 든 순간의 모양이 요청과 달라 0.3~0.7초
   만에 종료"였다. 틀린 동작을 즉시 실패로 보지 않고 제한 시간까지 기다리도록 바꾼 뒤 4회 중
   3회 통과.
2. **웹캠이 약 20fps로 도는데 임계값은 30fps 영상에서 프레임 수로 셌다.** 윈도우 600ms가
   874ms로 늘어났다. 상태 머신을 프레임 수가 아니라 관측 시각 기준으로 바꿔 해결(97% 통과).

⚠️ **현재 규칙 버전(`2026-09-16.opposite-first-fails`)의 실시간 세션은 아직 0회다.** 위 수치는
직전 버전들의 것이다.

### 알려진 한계 (요약, 상세는 8장)

- `NEG_halffist`의 OPEN_PALM 유입 20% 수용, `shape_confidence_min` 도출 실패
- 실측 참가자가 P01 한 명(실시간·웹캠 반례 전부)
- 이동 요청이 되돌아오는 획으로 반대 방향도 통과하던 문제 → "반대 방향 먼저면 실패" 규칙 적용
  (82.5% → 37.5%), 그래도 남아 있음
- 흔들기 5회 중 1회가 임계값을 0.004 차이로 넘어 이동으로 인정됨

---

## 3. 백엔드 (`backend/`)

### 기술 스택

| 항목 | 값 |
|---|---|
| 언어/런타임 | Python 3.11 |
| 웹 | FastAPI 0.141, uvicorn |
| DB | SQLite (파일) + SQLAlchemy 2.0, Alembic 마이그레이션 |
| 검증 | Pydantic v2 (camelCase JSON) |
| AI | numpy 1.26.4, torch 2.8.0 (CPU) |
| 테스트 | pytest 155개 통과 (테스트 파일 15개) |

DB는 SQLite지만 SQLite 전용 설정을 한 곳(`database.py`)에 모아 URL만 바꾸면 Postgres로 옮길 수 있게 했다.

### DB 스키마 요약

| 테이블 | 핵심 컬럼 | 성격 |
|---|---|---|
| `users` | id, name, department | 사용자 |
| `gestures` | id (G1~G5), label | 수어 암호 목록 |
| `enrollments` | user_id, gesture_id, take_no, **landmarks_json**, camera_width/height | **랜드마크 원본. 진실의 원천, 영구 보관** |
| `embeddings` | enrollment_id, **model_version**, vector(BLOB) | 파생 캐시. 버전별로 재생성 가능 |
| `templates` | user_id, gesture_id, model_version, **centroid**(재정규화 완료), take_count | 등록 임베딩의 중심 벡터 |
| `thresholds` | scheme, model_version, value, far, frr, basis, **is_active** | 운영점 3종. 활성 행만 바꿔 전환 |
| `auth_logs` | user_id, claimed/predicted_gesture_id, score, threshold, passed, fail_reason, 두 모델 버전, **landmarks_json**, latency_ms | 인증 이력 + 추가 학습 데이터 |
| `app_config` | key, value | 운영 중 바뀌는 설정(등록 회차 수 등) |

`claimed_gesture_id`(앱이 주장)와 `predicted_gesture_id`(분류 모델 판정)를 분리해서, 인증 실패가
제스처 오분류 때문인지 임베딩 불일치 때문인지 구분할 수 있게 했다.

### API 목록

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/verify` | 인증. 유사도 계산 → threshold 비교 → 이력 기록 |
| POST | `/enroll` | 등록. take 전부를 한 번에, 하나라도 불량이면 전체 거절 |
| GET | `/config` | 앱 등록 화면 구성값(회차 수, 촬영 시간, 손 제한) |
| GET / POST | `/users` | 사용자 목록 / 생성 |
| DELETE | `/users/{id}` | 사용자와 관련 데이터 전부 삭제(원본 포함) |
| GET | `/logs` | 인증 이력 (userId·passed 필터, 페이징) |
| GET | `/stats/monthly` | 월별 인증 건수 (KST, 0건인 달 포함) |
| GET | `/health` | 상태, 활성/로드 모델 버전, 활성 threshold |
| GET | `/admin/thresholds` | 운영점 목록 |
| POST | `/admin/threshold` | 활성 threshold 전환 (`far1`/`eer`/`far5`) |
| POST | `/admin/reindex` | 재색인 (dryRun 지원) |
| PATCH | `/admin/config` | `app_config` 변경 |

실패 응답은 전부 `{"detail": {"code", "reason", "message"}}` 형태이고, 앱은 `reason`으로 분기한다.

### AI 모델 연동 방식 — 별도 서버가 아니라 import

```
앱 ──HTTP──> FastAPI ──import──> ai.encoder ──> weights/*.pt
                                  (같은 파이썬 프로세스)
```

HTTP 서버를 하나 더 두지 않은 이유는 **왕복 비용과 운영 복잡도**다. `embed()` 한 번이 CPU에서
약 8ms인데 여기에 HTTP 왕복을 더할 이유가 없고, 프로세스가 둘이면 모델 버전이 어긋난 채로
도는 상황을 관리해야 한다. 모델은 startup에서 **한 번만** 로드한다(요청마다 로드하면 매번 수백 ms 낭비).

**경계는 명확하다.** AI 모듈은 임베딩과 제스처 분류만 하고, 코사인 유사도·centroid 생성·
threshold 비교는 백엔드 책임이다.

교체 시 형태가 달라질 수 있는 변환은 **`app/services/ai_gateway.py`의 `to_ai_input()` 한 곳**에만
있다. 실제로 v1.0.0을 받았을 때 백엔드가 가정한 입력 형태가 틀렸는데(아래 4장), 이 함수 하나만
고쳐서 해결했다.

### 재색인 구조와 필요성

**인코더가 바뀌면 저장된 임베딩은 전부 무효가 된다.** 다른 벡터 공간의 값이라 코사인 유사도가
의미를 잃는다. 사용자를 다시 불러 등록시킬 수는 없으므로, 랜드마크 원본을 새 인코더에 다시
통과시켜 임베딩과 템플릿을 재생성한다. 실제로 AI팀이 D=169(pose 포함) → D=127(hand only)로
바꾸면서 이미 한 번 일어난 일이다.

```
1단계 (읽기 + 임베딩)   원본을 새 인코더에 통과. DB에 쓰지 않는다. 실패 건을 모은다.
2단계 (쓰기 + 전환)     실패 0건일 때만, 한 트랜잭션으로 저장하고 활성 버전 전환
```

- 한 건이라도 실패하면 **아무것도 저장하지 않고** 전환하지 않는다. 실패 목록을 응답에 담는다.
- 옛 버전의 임베딩·템플릿은 지우지 않는다(되돌릴 때 사용).
- 재색인 전에는 `/verify`가 503 `model_version_mismatch`로 거부한다. 벡터 공간이 다른 템플릿과
  비교하면 **에러 없이 틀린 점수**가 나오기 때문이다.

실제 실행 결과(스텁 → v1.0.0): 등록 9건 처리, 템플릿 3개 재생성, 실패 0, 201ms, 활성 버전 전환 완료.

### threshold 3종 운영

| 기준 | 값 | 검증셋 FAR | 검증셋 FRR |
|---|---|---|---|
| **far1 (기본 활성)** | 0.6275163888931274 | 0.95% | 4.29% |
| eer | 0.4360462427139282 | 2.86% | 2.86% |
| far5 | 0.27212807536125183 | 5.00% | 2.14% |

DB에 3행이 모두 들어 있고 `is_active`만 바꿔 전환한다. **요청마다 활성 행을 읽으므로 서버 재시작이
필요 없다.** 시연 리허설에서 거부가 잦으면 `POST /admin/threshold {"basis": "eer"}` 한 번으로 내릴 수 있다.
`auth_logs.threshold`에 시도마다 실제로 쓴 값이 남아 나중에 해석할 수 있다.

---

## 4. AI 모델 (외부 전달본)

### 받은 것

2026-09-16 수령, `backend/ai/`에 반영.

| 항목 | 값 |
|---|---|
| 인증 모델 | `handonly-supcon-v1.0.0` (`weights/auth_handonly_supcon_v1.pt`, 515KB, 125,099 파라미터) |
| 제스처 모델 | `handonly-gesture-1dcnn-v1.0.0` (`weights/gesture_handonly_1dcnn_v1.pt`, 471KB, 114,664 파라미터) |
| 코드 | `encoder.py`, `features.py`, `model_defs.py` |
| 부속 | `preprocess.json`, `thresholds.json`, `manifest.json`, `calibration_report.md`, `cpu_benchmark.json`, `smoke_test.py` |
| 의존성 | numpy 1.26.4, torch 2.8.0 |

`smoke_test.py` 통과, `scripts/verify_ai_release.py` 15개 항목 전부 PASS.
⚠️ `manifest.json`의 sha256이 `README.md`·`thresholds.json` 두 파일과 불일치한다(질문 반영 후
문서만 수정하고 manifest를 재발행하지 않은 것으로 보임). 가중치를 포함한 나머지 11개는 일치.

### 확정된 계약값

| 항목 | 값 |
|---|---|
| 임베딩 | 128차원, float32, **L2 정규화된 상태로 반환** |
| 내부 feature | `[T=32, D=127]` (손 좌표 63 + 속도 63 + 검출 마스크 1) |
| 최소 입력 | 프레임 8개 이상, 유효 손 프레임 8개 이상, 첫–마지막 간격 750ms 이상 |
| 보간 | 8~31프레임은 AI 모듈이 T=32로 보간 (**백엔드가 패딩하지 않는다**) |
| 카메라 크기 | width/height 필수 (학습 때 종횡비 보정) |
| 손 | **오른손만** (왼손 프레임은 거절) |
| threshold 방식 | Global 하나 (사용자별·제스처별 아님) |
| 등록 | 개인 제스처 1개 × 3회 |
| 동시 호출 | 내부 RLock으로 안전 (백엔드 락 불필요) |
| 추론 속도 | 우리 환경(Windows/CPU) `embed()` 평균 7.95ms, `classify_gesture()` 10.09ms |

백엔드가 가정했던 입력 형태는 실제와 달랐고(카메라 크기가 최상위 `width`/`height`, 좌표 키가
`landmarks`), 교체할 때 `to_ai_input()`을 고쳐 맞췄다. `InvalidSequenceError`도 `encoder.py`가
아니라 `features.py`에만 있어 import 경로를 조정했다.

### 성능 수치 — ⚠️ 검증셋과 신규 사용자를 반드시 구분할 것

| 운영점 | | 검증셋 FAR | 검증셋 FRR | **신규 사용자 FAR** | **신규 사용자 FRR** | 신규 정확도 |
|---|---|---|---|---|---|---|
| FAR≤1% | **배포 기본값** | 0.95% | 4.29% | **7.16%** | **40.54%** | 83.04% |
| EER | | 2.86% | 2.86% | 12.50% | 28.72% | 82.74% |
| FAR≤5% | | 5.00% | 2.14% | 20.51% | 18.58% | 80.06% |

- **검증셋**: 학습에 참여한 사용자(P01~P07)의 다른 세션. threshold를 이 데이터로 골랐다.
- **신규 사용자**: 학습에 전혀 없던 사용자(P08~P10). **실제 배포 상황에 해당한다.**

> **배포 기본값에서 신규 사용자 FRR은 40.54%다. 본인이 10번 중 4번 거부된다는 뜻이다.**
> AI팀 원문: "이 연구 모델은 배포 환경에서 FAR 1%를 보장하지 못한다. 프로젝트 시연용으로는
> 적합하지만, 안전이 걸린 시스템이나 실제 출입 통제의 유일한 수단으로는 적합하지 않다."

발표·보고서에서 "FAR 1% 수준의 출입 통제"로 표현하면 안 된다. 등록 회차를 3회로 정한 것도 이
데이터에 근거한다(1회 → 신규 정확도 73.31%, 3회 → 82.74%, 4회 → 82.84%로 3회에서 포화).

### 제스처 분류기를 판정에서 뺀 결정

**결정: `USE_GESTURE_CLASSIFIER=false`. 앱이 보낸 `gestureId`로 템플릿을 조회하고, 분류 결과는
`auth_logs.predicted_gesture_id`에 기록만 한다.** (2026-09-16 팀 결정)

근거는 AI팀 레포를 직접 확인한 결과다.

| 확인한 것 | 내용 |
|---|---|
| 분류기 출력 | `classes = ['G1','G2','G3','G4','G5']`, `num_classes=5` |
| "미분류" 출력 | **없음.** softmax argmax로 항상 5개 중 하나를 고른다. 신뢰도 임계값도 없음 |
| 결과 | 학습에 없는 새 동작도 G1~G5 중 하나로 분류된다 |
| 명세 기본 흐름 | 분류 결과로 템플릿을 조회 → 새 동작은 구조적으로 등록·인증 불가 |

분류를 판정 경로에서 빼면 **사용자별 개인 제스처로 확장할 때 백엔드 구조를 바꾸지 않아도 된다.**
조회 키가 문자열이라 AI팀이 개인 제스처 ID 방식을 주면 값만 바뀐다. 분류 결과를 계속 기록하는
이유는 오분류 분석용이고, AI팀도 이를 분석 항목으로 두고 있다.

⚠️ 이 결정은 **AI팀의 공식 답변이 아니라 우리 팀의 운영 결정**이다. 릴리스 문서 어디에도 direct
조회 가능 여부에 대한 언급이 없어 확인 요청 중이다.

---

## 5. 앱 (`lib/`)

### 화면 구성

| 화면 | 역할 |
|---|---|
| `home_screen` | 사용자 선택, **수어 암호(G1~G5) 선택**, 인증/등록/이력 진입, 현재 모드 칩 |
| `auth_screen` | 가이드 원 + 카메라 프리뷰 + 손 뼈대 오버레이, 상태 머신(손 탐색 → 카운트다운 → 녹화 → 전송) |
| `enroll_screen` | 같은 캡처 UI + 회차 진행 인디케이터(서버가 정한 회차 수) |
| `result_screen` | 통과/거부, 유사도·기준값·지연 칩, 사유별 안내 |
| `admin_screen` | 인증 이력 표 + 월별 인증 현황 차트 |

캡처 절차(`CaptureSession`)는 인증과 등록이 공유한다. 랜드마크 소스는 인터페이스 뒤에 숨겨
실기기(`OnDeviceLandmarkSource`)와 테스트용 가짜(`FakeLandmarkSource`)를 바꿔 끼운다.

### 백엔드 연동 상태

| 항목 | 상태 |
|---|---|
| 경로 | `/verify`, `/enroll`, `/config`, `/logs`, `/stats/monthly` (`/v1` 접두사 제거 완료) |
| 요청 규격 | `gestureId`, `camera.width/height`, `takes[{takeNo, ...}]` 전부 반영 |
| 응답 규격 | `score`를 nullable로 처리(서버가 비교하지 않으면 null) |
| 에러 처리 | 사유 코드 18종 → 안내 문구 매핑, 등록 실패는 회차 번호까지 표시 |
| 서버 설정 | 앱 시작 시 `GET /config` 호출, 등록 회차 수를 서버 값으로 사용 |
| 전환 | `kUseMockApi` 기본 `true`(목). `false`로 바꾸고 `--dart-define=SIGNID_API_BASE=...`로 주소 주입 |
| 검증 | `flutter analyze` 무결점, `flutter test` 35개 통과. 실제 서버 대상 연결 확인 완료 |

실제 백엔드(`handonly-supcon-v1.0.0`)에 앱의 HTTP 클라이언트로 붙여 확인한 결과:
등록 → 같은 입력 인증 시 score 1.0 통과, 무관한 입력 -0.238 거부, 미등록 암호 `no_template`(score null),
프레임 부족 422 `too_few_frames`, 왼손 422 `wrong_hand`.
⚠️ 이 확인은 **합성 랜드마크**로 한 것이며, 실기기에서 사람이 수행한 동작으로는 하지 않았다.

### 오른손 제한과 handedness 미지원 ⚠️

모델이 오른손만 학습했고, 서버는 프레임의 `handedness`로 왼손을 거른다(422 `wrong_hand`).
그런데 **앱이 쓰는 `hand_landmarker` 3.0.1 플러그인은 좌/우를 제공하지 않는다.**

```dart
class Hand {
  final List<Landmark> landmarks;   // 이게 전부. 좌/우도 신뢰도도 없다
}
```

이전 앱은 `'Right'` 고정값을 보내고 있었지만, AI 릴리스 문서가 이를 명시적으로 금지한다
("a hard-coded `Right` label must not be used to disguise actual left-hand input"). 실제 왼손을
오른손으로 위장해 보내면 인식률만 조용히 떨어진다. 그래서 **handedness 키 자체를 빼고 보낸다.**

> **결과: 왼손으로 인증해도 서버가 걸러내지 못한다.** 현재 방어선은 인증·등록 화면의
> "오른손을 사용해주세요" 안내뿐이다.

`HandFrame.handedness`가 nullable이라, 플러그인이 값을 주게 되면 소스 코드 한 줄만 고치면
서버 검사가 자동으로 살아난다.

---

## 6. 주요 설계 결정과 근거

| 결정 | 근거 |
|---|---|
| **랜드마크 원본 영구 저장 + 임베딩은 캐시** | 인코더가 바뀌면 임베딩은 전부 무효다. 사용자를 다시 부를 수 없으므로 원본만이 재생성의 근거다. 실제로 D=169→D=127 교체가 이미 일어났다 |
| **threshold를 서버가 소유** | 앱에 박으면 운영 중 조정이 불가능하다. 시연에서 거부가 잦으면 재배포 없이 EER로 내릴 수 있어야 한다 |
| **제스처 분류기를 판정에서 제외** | 분류기가 G1~G5 닫힌 집합이고 "미분류"가 없다. 판정에 쓰면 개인 제스처로 확장할 수 없다. 기록은 남겨 오분류를 분석한다 |
| **AI 모델을 import로 사용** | `embed()` 8ms에 HTTP 왕복을 더할 이유가 없고, 프로세스가 둘이면 모델 버전 불일치를 따로 관리해야 한다 |
| **centroid 평균 후 재정규화** | 재정규화를 빠뜨리면 유사도가 전부 미묘하게 낮아지고 threshold가 어긋나는데 **에러는 나지 않는다.** 테스트로 고정했다 |
| **전처리를 AI 모듈에만 둠** | 앱이나 백엔드가 같은 계산을 다시 구현하면 학습 때와 달라져 정확도가 조용히 깎인다 |
| **모델 버전 불일치 시 503 거부** | 벡터 공간이 다른 템플릿과 비교하면 에러 없이 틀린 점수가 나온다. 비교하지 않고 거부하는 편이 낫다 |
| **임계값을 코드에 박지 않음 (안티스푸핑)** | 상식으로 정한 값은 근거가 없다. 분포에서 도출하고, 도출 실패는 `null`로 남겨 드러낸다 |
| **handedness를 보내지 않음** | 모르는 값을 'Right'로 채우면 왼손을 위장하는 셈이다. 안 보내는 쪽이 정직하고, 플러그인이 주면 바로 살아난다 |

---

## 7. 현재 상태와 남은 작업

| 영역 | 상태 | 내용 |
|---|---|---|
| 안티스푸핑 임계값 도출 | ✅ 완료 | 5명 × 26개 영상, SPEC 목표 10개 중 9개 달성 |
| 안티스푸핑 실시간 프로토타입 | ✅ 완료 | 웹캠 77회, 최신 직전 규칙에서 33/34 통과 |
| 백엔드 API | ✅ 완료 | 12개 엔드포인트, pytest 155개 통과 |
| AI 모델 교체 | ✅ 완료 | `handonly-supcon-v1.0.0`, 검증 스크립트 15항목 PASS, 재색인 성공 |
| 앱 ↔ 백엔드 규격 | ✅ 완료 | `flutter analyze` 무결점, 테스트 35개, 실서버 연결 확인 |
| 관리자 화면 데이터 | ✅ 완료 | 시드 스크립트(사용자 6명 + 5개월 이력 223건) |
| **앱 실서버 전환** | 🔶 진행 중 | `kUseMockApi`는 아직 `true`. 실기기에서 전환 테스트 필요 |
| **실기기 인증 검증** | ❌ **미착수** | 아래 참고 |
| 안티스푸핑 ↔ 본 인증 통합 | ❌ 미착수 | Challenge 모듈이 독립 프로토타입 상태. 앱·백엔드와 연결 안 됨 |
| 최신 규칙 실시간 검증 | ❌ 미착수 | `opposite-first-fails` 규칙의 실시간 세션 0회 |
| 개인 제스처 지원 | ❌ 미착수 | AI팀 새 릴리스 대기 |
| 운영 보안 | ❌ 미착수 | `/admin/*`, `DELETE /users`에 접근 제어 없음 |
| 개인정보 보관 기한 | ❌ 미정 | 명세가 README 명시를 요구하나 팀 결정 대기 |

### ⚠️ 가장 큰 미확인 사항

> **실기기에서 실제 사람의 동작이 threshold를 넘는지 아직 검증하지 않았다.**

지금까지의 모든 인증 검증은 **합성 랜드마크**로 했다. 확인한 것은 "같은 입력이면 유사도 1.0,
무관한 입력이면 낮다"는 배선 수준의 성질이고, **사람이 같은 동작을 두 번 했을 때 유사도가
0.6275를 넘는지는 한 번도 재지 않았다.** AI팀 수치(신규 사용자 FRR 40.54%)는 그들의 데이터셋에서
나온 것이지 우리 앱–카메라 조합에서 잰 것이 아니다.

이것이 시연 성패를 좌우한다. 확인해야 할 것:

1. 팀원 몇 명이 앱으로 등록 → 인증했을 때 통과율
2. 통과한다면 어느 운영점에서 통과하는지(far1 / eer / far5)
3. 앱 카메라 해상도와 fps가 AI팀 학습 조건과 얼마나 다른지
4. MediaPipe 버전 차이(앱 네이티브 `tasks-vision:0.10.29` ↔ AI팀 Python `mediapipe 0.10.14`)가
   좌표에 미치는 영향

---

## 8. 알려진 한계 (전체)

### 모델 성능

| 한계 | 상세 |
|---|---|
| 신규 사용자 FRR 40.54% | 배포 기본값(far1)에서 본인이 10번 중 4번 거부된다 |
| 신규 사용자 FAR 7.16% | 검증셋 0.95%의 7.5배. 출입 통제 수준이 아니다 |
| 학습 데이터 규모 | 10명(학습 7 + 미지 3), 데이터셋 1,955 샘플 |
| 개인 제스처 불가 | 분류기가 G1~G5 닫힌 집합. 판정에서 빼서 우회했으나 모델 자체는 5개 동작으로 학습됨 |

### 백엔드

| 한계 | 상세 |
|---|---|
| 인코더 교체 중 인증 중단 | 프로세스에 인코더가 하나라 재색인이 끝날 때까지 `/verify`는 503 |
| 접근 제어 없음 | `/admin/*`, `DELETE /users`에 인증이 없다. 외부 노출 금지 |
| SQLite 쓰기 동시성 | 쓰기가 하나씩(WAL + busy_timeout 5초). 시연 규모에는 충분 |
| 재등록 시 옛 원본 삭제 | `UNIQUE(user_id, gesture_id, take_no)` 제약 때문. 보관이 필요하면 스키마 변경 필요 |
| 재색인 중 재등록 | 같은 사용자가 동시에 재등록하면 재색인이 실패할 수 있다(롤백되어 데이터는 안전) |
| 왼손 검사 의존성 | 앱이 `handedness`를 보내야만 서버가 왼손을 거른다 |

### 앱

| 한계 | 상세 |
|---|---|
| handedness 미제공 | 플러그인이 좌/우를 주지 않아 **서버가 왼손을 걸러내지 못한다.** 화면 안내가 유일한 방어선 |
| 검출 신뢰도 미제공 | 플러그인이 score를 주지 않아 설정한 최소 신뢰도(0.6)를 하한으로 기록 |
| Android 전용 | `hand_landmarker`가 Android만 지원 |
| 실서버 미전환 | `kUseMockApi = true`가 기본. 실기기 전환 테스트 미실시 |

### 안티스푸핑

| 한계 | 상세 |
|---|---|
| `NEG_halffist` 목표 미달 | OPEN_PALM 유입 20.0% 수용(막으면 정상 정답률 99.9%→89.4%) |
| `shape_confidence_min` 도출 실패 | 분포가 겹쳐 `null` + `_unresolved` |
| 실시간 실측 1명 | 실시간 77회·웹캠 반례 15회 전부 P01 |
| 최신 규칙 미검증 | `opposite-first-fails` 규칙의 실시간 세션 0회 |
| 반대 방향 통과 잔존 | "반대 먼저면 실패"로 82.5% → 37.5%, 재시도 틈으로 여전히 통과 가능 |
| 흔들기 여유 없음 | 5회 중 1회가 임계값을 0.004 차이로 넘어 이동으로 인정 |
| `max_lost_frames` 미도출 | 5명 영상에 중간 끊김이 1건뿐. 임시값 38 사용(측정값 아님) |
| 1단계 이탈 관문 없음 | 3단계 중 1단계는 직전 모양 검사가 없다(비용 대비 판단) |
| 파일럿 표본 | 참가자당 동작별 영상 1개라 백분위 추정이 불안정 |
| 타이밍 근거 | 파일럿 수행 시간 기반. 요청을 읽고 반응하는 시간은 실사용 데이터로 다시 재야 함 |
| 20fps 제약 | MediaPipe(model_complexity 1)가 프레임당 50~57ms. SPEC 설정을 바꾸지 않으면 30fps 불가 |

### 통합·운영

| 한계 | 상세 |
|---|---|
| 실기기 인증 미검증 | **7장 참고. 가장 큰 미확인 사항** |
| Challenge 미통합 | 안티스푸핑이 독립 프로토타입. 앱·백엔드와 연결되지 않음 |
| MediaPipe 버전 차이 | 앱 네이티브 `tasks-vision 0.10.29` ↔ 안티스푸핑·AI팀 Python `mediapipe 0.10.14` |
| manifest 해시 불일치 | AI 릴리스의 `README.md`·`thresholds.json` 2개가 manifest와 불일치 |
| 개인정보 보관 기한 미정 | 손 랜드마크는 생체인식정보로 볼 여지가 있다. 기한 결정 필요 |

---

## 부록: 작업 이력 (git log 요약)

| 기간 | 커밋 | 내용 |
|---|---|---|
| 2026-08-24 ~ 08-25 | 6개 | Flutter 앱: 프로젝트 생성, 오버레이, 인증·등록·관리자 화면, 실기기 검증 |
| 2026-09-07 ~ 09-08 | 9개 | 안티스푸핑: 랜드마크 추출 → 분포 측정 → 임계값 도출 → 규칙 검증 → 실시간 프로토타입 |
| 2026-09-16 | 3개 | 안티스푸핑: 5명 데이터 재도출, 대각선 판정 기준 변경, 반대 방향 규칙 |
| 2026-09-16 | 12개 | 백엔드: 명세 → 11단계 구현 (구조·모델·스텁·스키마·등록·인증·조회·운영·시드·테스트·문서) |
| 2026-09-16 | 1개 | AI 릴리스 `handonly-supcon-v1.0.0` 교체 |
| 2026-09-16 | 1개 | 앱을 백엔드 규격에 맞춤 |

총 36개 커밋. 백엔드·AI 교체·앱 수정 작업은 `backend-v1` 브랜치에 있다(main 미병합).

이 문서의 수치 근거: `challenge_response/configs/challenge_config.json`(임계값과 `_source`),
`challenge_response/data/sessions/results.csv`(실시간 77회), `challenge_response/data/landmarks/`(130개),
`backend/ai/thresholds.json`·`calibration_report.md`(성능), `backend/ai/manifest.json`(파일 구성),
`pytest`(백엔드 155) / `flutter test`(앱 35) 실행 결과.
